import React from 'react';
import './header.css';

const headerStyle = {
  color: 'white',
};

const Header = () => (

  <div className="header-container">
    <h2 style={headerStyle}>
      Reunite
    </h2>

  </div>
);

export default Header;
