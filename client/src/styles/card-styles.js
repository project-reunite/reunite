const cardStyle = {
  borderRadius: '20px',
  // boxShadow: '3px 3px #888888',
};

const cardFooterStyle = {
  borderRadius: '20px',
  // boxShadow: '3px 3px #888888',
};

const cardImageStyle = {
  borderRadius: '20px',
  // boxShadow: '3px 3px #888888',
};

module.exports = {
  cardStyle,
  cardFooterStyle,
  cardImageStyle,
};
