{
  "name": "reunite",
  "version": "0.1.0",
  "description": "App for reuniting families separated by natural disasters",
  "main": "index.js",
  "scripts": {
    "test": "(cd server && npm test) && (cd client && npm test)",
    "test:full": "(cd server && npm run test:full) && (cd client && npm test)",
    "install": "(cd server && npm install); (cd client && npm install)",
    "start:client": "cd client && npm start & wait-on http://localhost:41002",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run"
  },
  "repository": {
    "type": "git",
    "url": "git@github.ibm.com:ProjectReunite/reunite.git"
  },
  "keywords": [
    "Watson"
  ],
  "author": "Project Reunite",
  "license": "Apache-2.0",
  "devDependencies": {
    "cypress": "^3.3.1"
  },
  "dependencies": {
    "wait-on": "^3.2.0"
  }
}
